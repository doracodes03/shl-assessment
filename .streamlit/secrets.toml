# Streamlit secrets for local development
# This file is optional; it's created to avoid Streamlit runtime errors when no secrets exist.
# Fill in real keys here or keep values blank for local dev.

backend_url = "http://localhost:8000/chat"

# Optional provider keys (fill when available)
# GROK_API_KEY = ""
# GEMINI_API_KEY = ""
